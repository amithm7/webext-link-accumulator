# Link Accumulator

## Testing Server

- Firefox:
  - Navigate to `about:debugging`
  - Select `manifest.json` file

## Build Artefacts

- `cd src/ ; zip -r -FS ../build/ammoliac_vd.e.v.xpi *; cd ..;`
